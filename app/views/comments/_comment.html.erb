<% post.comments.each do |comment| %>
  <%= comment.user.username %>:
  <%= comment.body %>
  <%= link_to 'Delete', comment_path(comment), data: { turbo_method: :delete } if current_user == comment.user %>
  <br>
<% end %>
