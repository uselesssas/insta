<% @posts.each do |p| %>
  <%= p.user.username %>
  <%= p.description %>
  <%= link_to 'DELETE', post_path(p), data: { turbo_method: :delete } %>
  <% if p.images? %>
    <% p.images.each do |i| %>
      <%= link_to image_tag(i.thumb.url, size: '470x587'), post_path(p) %>
    <% end %>
  <% end %>
<% end %>
